---
import Layout from "@/layouts/Layout.astro";
import { GeneratePage } from "@/components/generate";

// Auth guard is handled by middleware - this page is protected by default
// If user is not authenticated, middleware will redirect to /auth/login
const user = Astro.locals.user;

// User is guaranteed to be authenticated here (middleware handles redirect)
// Pass user to component if needed
---

<Layout title="Generate Flashcards">
  <GeneratePage client:load />
</Layout>
