---
import Layout from "@/layouts/Layout.astro";
import { RegisterForm } from "@/components/auth/RegisterForm";

// Check if user is already logged in (from middleware)
const user = Astro.locals.user;

if (user) {
  // User is already authenticated, redirect to app
  return Astro.redirect("/generate");
}
---

<Layout title="Zarejestruj się - SmartFlash">
  <div class="min-h-screen flex items-center justify-center bg-muted/30 py-12 px-4">
    <RegisterForm client:load />
  </div>
</Layout>
